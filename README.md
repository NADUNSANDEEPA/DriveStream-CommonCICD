# Shared CI Workflow (`common.yml`)

This reusable GitHub Actions workflow centralizes the shared CI/CD logic used by both the DriveStream Backend and Frontend pipelines.

It helps eliminate duplicated YAML configuration and ensures consistent CI standards across all services.

---

# Location

```text
.github/workflows/common.yml